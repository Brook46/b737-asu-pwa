{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.js|*.mjs) node --check \"$f\" || exit 2 ;; esac",
            "timeout": 15,
            "statusMessage": "Syntax-checking JS"
          }
        ]
      }
    ]
  }
}
