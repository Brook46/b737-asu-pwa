.DS_Store
*.log
__pycache__/
*.pyc
.venv/
node_modules/

# Editor
.vscode/
.idea/
*.swp

# Local dev caches
/tmp/
/qlmtest/

# Claude local runtime (keep launch.json + shared settings, ignore rest)
.claude/*
!.claude/launch.json
!.claude/settings.json

# Source PDFs are not redistributed by default
*.pdf
QRH.pdf

# ...except the bundled seed PDF that ships with the app
!pdf-knowledge-pwa/seed-data/tanchum-737-upgrade.pdf

# Generated by pdf-knowledge-pwa/_build_seed.py from _tanchum_structure.json
pdf-knowledge-pwa/_tanchum_seed.js

# Local iOS wrapper experiment — not referenced by anything deployed
ios/

# Xcode / native build artifacts
ios-flightcard/build/
ios-flightcard/DerivedData/
*.xcuserstate
xcuserdata/

# Seeded design-canvas output (~2.5MB editor payload) — the design lives in the
# published Artifact; only the source artboards are worth versioning.
design/**/sky-club-polish.html
